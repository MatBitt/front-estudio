import { QuebraDeLinhaPipe } from './quebra-de-linha.pipe';

describe('QuebraDeLinhaPipe', () => {
  it('create an instance', () => {
    const pipe = new QuebraDeLinhaPipe();
    expect(pipe).toBeTruthy();
  });
});
